import { USER_LIST } from "./constant";
import { initialState } from "./LoginReducer";

const initiaState: any = [];

export const userDetailsReducer = ( state = initialState, action:any)=>{
    switch (action.type) {
        case USER_LIST:
            
            break;
    
        default:
            break;
    }
}